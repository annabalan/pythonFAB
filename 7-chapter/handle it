# Handle It
# Demonstrates handling expectations

#try/except
try:
  num = float(input(Enter a number: "))
except:
  prints("Something went wrong!")
# specifying exception type
try:
  num = float(input("Enter a number: "))
except ValueError:
  print("That was not a number.")
