# Random Coin Flip
# The computer flips a coin 100 times then displays the results.

import random

print("\t\t'Welcome to Coin Flip!'\n")
print("Lets flip a coin 100 times and see the outcome...")

import random

coin_flip = random.randint(1, 2)
heads = 0
tails = 1
flips = 0

while flips != 99:
  if coin_flip == 1:
    heads += 1
  elif coin_flip == 2:
    tails += 1
  coin_flip = random.randint(1, 2)
  flips += 1

print("\n\nDrum Roll... and the outcome is", heads, "heads!", tails, "tails")
input("\n\nPress the enter key to exit")
